# SQLite
SQL queries to work with SQLite in Python -- these files are text with lines of SQL code

Access individual files in this repo to read them as text files containing one or more SQL statements
